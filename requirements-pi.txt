# Raspberry Pi Linux Requirements
# Optimized for ARM64 architecture (Raspberry Pi 4/5)

# Core dependencies - lightweight versions for Pi
fastapi>=0.100.0
numpy>=1.24.0
opencv-python>=4.8.0  # Will use system opencv if available
pydub>=0.25.1
pynput>=1.8.1
python-dotenv>=1.1.1
pyyaml>=6.0
rich>=14.2.0
uvicorn>=0.23.0

# Reachy SDK and toolbox
reachy-mini-dances-library @ git+https://github.com/pollen-robotics/reachy_mini_dances_library
reachy-mini-toolbox[vision] @ git+https://github.com/pollen-robotics/reachy_mini_toolbox
reachy-mini[mujoco]>=1.0.0rc5

# Audio - Install via setup_pi.sh if needed (optional)
# librosa>=0.11.0  # Optional - install separately if using voice features
# pyttsx3>=2.90  # Optional - install separately if using voice features

# OpenAI - may need to build on Pi
openai>=2.4.0  # Removed [voice-helpers] to avoid binary dependencies

# Dev dependencies
pillow>=12.0.0
pytest>=9.0.1

# NOTES FOR PI INSTALLATION:
# 1. Use setup_pi.sh for automated installation, or:
#
# 2. Manual setup:
#    sudo apt install -y python3-opencv python3-numpy python3-yaml python3-pil
#    pip install -r requirements-pi.txt
#
# 3. For audio support (optional):
#    sudo apt install -y portaudio19-dev libsndfile1 libsndfile1-dev ffmpeg
#    pip install librosa pyttsx3 pydub
#
# 4. We use OpenCV SFace model (not face-recognition library)
#    Already implemented in src/vision/face_encoder.py
